# 许吃上线清单

> 当前代码基于 main，目标是微信小程序正式发布。仓库已经完成核心业务、推荐埋点、并发一致性和基础安全收口；上线前仍有必须由小程序管理员在微信公众平台/云开发控制台完成的配置项。

## 1. 必须配置
- 将根目录 `config.js` 的 `YOUR_ENV_ID` 替换为正式云开发环境 ID。
- 在微信开发者工具使用正式小程序 AppID 导入项目。
- 云开发环境与正式小程序 AppID 绑定。
- 部署 `cloudfunctions/*` 下的云函数，至少包含 api、feed、publish、groupbuy、recommendV2、dishHobbyReviews。
- 创建 `database/init.json` 中列出的集合和索引，并按 `database/rules.json` 配置生产权限。
- 发布前检查云存储域名、业务域名、地图/定位等微信平台配置。

## 2. 数据初始化
不要在正式环境依赖用户请求自动造测试数据。首次上线建议在云开发控制台或一次性初始化脚本导入真实的店铺、菜品和标签数据，然后关闭/移除测试种子逻辑。

## 3. 合规与审核
- 完成隐私保护指引，并在小程序中提供隐私政策入口。
- 发布、评论、头像/昵称等用户生成内容接入微信内容安全能力，并提供举报/删除处理机制。
- 若启用定位，说明定位用途并只在必要时请求授权。
- 若以后接入付费团购/支付，再单独完成微信支付、退款、订单、核销和商户资质链路；当前团购模块不是支付订单系统，不应冒充已付款团购。

## 4. 上线验收
- 新用户首次打开、登录、授权失败、网络失败均可正常使用。
- 首页推荐、附近、搜索、发现、发布、菜品详情、收藏、许吃、评论、关注、同好、团购全部走正式云函数。
- 快速连续点击不会产生重复关注/收藏/用户当前状态。
- 推荐曝光、点击、决策、导航事件正常写入 `user_action_events`。
- 正式环境不暴露 openid 等内部身份字段。
- 检查所有云函数错误日志和调用耗时。

## 5. 发布顺序
1. 备份正式数据库。
2. 配置正式环境 ID 和 AppID。
3. 创建数据库集合/索引/权限。
4. 导入真实数据。
5. 部署云函数。
6. 微信开发者工具真机测试。
7. 上传代码并提交审核。
8. 审核通过后再发布。
9. 发布后观察云函数错误率、推荐事件量、搜索失败率、发布失败率和团购异常。

## 6. 当前产品边界
第一版正式上线建议聚焦“今天吃什么”：推荐、附近、菜品、许吃/有条件/不许吃、吃过/再吃、收藏、发现和轻量同好。商家认领和团购先作为增强能力；支付、广告竞价、复杂 AI 和大规模推荐模型放到后续版本。
